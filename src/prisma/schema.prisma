generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Notices {
  Id          Int     @id @default(autoincrement())
  Title       String?
  Description String?
  Details     String?
}
